- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/Brewfile: Brewfile
    ~/.zsh.after: zsh.after
    ~/bin: bin
    ~/.gitconfig: gitconfig
    ~/.config/nvim/init.vim:
      create: true
      path: init.vim

- shell:
  - [git submodule update --init --recursive, Installing submodules]
